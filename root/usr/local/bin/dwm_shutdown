#!/usr/bin/env bash

declare -a options=("shutdown
reboot")

choice=$(echo -e "${options[@]}" | dmenu -p "Follow your heart!")

case "$choice" in
    shutdown)
        shutdown now
    ;;
    reboot)
        reboot
    ;;
esac