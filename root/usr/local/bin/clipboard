#!/usr/bin/env bash

old_xsel_b=""

while true; do
  clipnotify

  # Do nothing if clipboard is equal to copy 
  if [[ $(xsel -b) != $old_xsel_b ]]; then
    old_xsel_b=$(xsel -b)
    echo -n "$(xsel -b)" | xsel -bi
  fi

  # Do nothing for secondary clipboard
  # echo -n "$(xsel)" | xsel -i
done