#!/usr/bin/env bash

#
# !! Can only copy text !!
#

while true; do
	clipnotify

	clipboard=$(xclip -o -selection clipboard)
	primary=$(xclip -o -selection primary)

	printf "$clipboard" | xclip -selection clipboard
	printf "$primary" | xclip -selection primary
done